defaults:
  - dataset: fake_iterable
  - resources: cpu
  - reproducibility: default
  - data_loading: torch
  - experiment: default
  - _self_
experiment:
  exp_name: mlp_iterable_dataset
grid:
  defaults:
    - optimizer: adam
    - search/iterable@_here_
    - _self_
  model: mlwiz.model.MLP
  checkpoint: true
  shuffle: true
  batch_size: 32
  epochs: 3
  dim_embedding:
    - 5
    - 10
